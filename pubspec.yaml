name: locale_link_call
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"
# 这表明项目创建时使用的 Dart SDK 版本范围是 >=2.17.5 <3.0.0 。根据 Dart 和 Flutter 的版本对应关系：

# - Flutter 3.0.0 发布于 2022 年 5 月，对应的 Dart 版本是 2.17.0
# - Flutter 3.3.0 发布于 2022 年 8 月，对应的 Dart 版本是 2.18.0
# 由于项目使用的是 Dart >=2.17.5 <3.0.0 ，可以推断项目很可能是在 Flutter 3.0.0 到 3.2.x 版本之间 创建的。
# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter



    # The following adds the Cupertino Icons font to your application.
    # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.5
  volume_watcher : ^2.0.1
  event_bus: ^2.0.0
  dio: ^4.0.6 # http请求库
  shared_preferences: ^2.0.15 # 本地数据存储
  timeago: ^3.3.0
  w_popup_menu: ^1.0.0
  # 空安全版本 最新版本请查看  https://pub.flutter-io.cn/packages/get
  get: ^4.6.5
  #最后支持的一个版本
  mqtt_client: ^9.6.8
  device_info: ^2.0.3
  platform_device_id: ^1.0.1
  #luyou
  fluro: ^2.0.3
  #er wei ma
  qr_flutter: ^4.0.0
  permission_handler: ^9.2.0
  agora_rtc_engine: ^6.0.0
  uuid: 3.0.6
  lpinyin: ^2.0.3
  scrollable_positioned_list: ^0.3.5
  #  azlistview: ^2.0.0
  #  flutter_local_notifications: ^9.8.0+1
  redux: ^5.0.0
  flutter_redux: ^0.10.0
  #  xftts_fluttify: ^0.2.1
  flutter_tts: ^3.5.3
  flutter_update_dialog: ^2.0.0
  webview_flutter: ^4.8.0
  geolocator: ^9.0.0

#  bot_toast: ^4.0.3 不兼容网页
  toast: ^0.3.0
  fluttertoast: ^8.1.1
  oktoast: ^3.3.1
  flutter_styled_toast: ^2.1.3
  flutter_smart_dialog: ^4.9.0

  # Flutter 屏幕适配 https://github.com/OpenFlutter/flutter_screenutil
  flutter_screenutil: ^5.6.0 # 屏幕适配方案
  url_launcher: ^6.1.6
  #  flutter_webrtc: ^0.9.5

  #  scoped_model: ^1.1.0
  #  adhara_socket_io: ^1.0.1+1
  path_provider: ^2.0.11
  audioplayers: ^1.1.1 #A Flutter plugin to play multiple simultaneously audio files, works for Android, iOS, Linux, macOS, Windows, and web.
  json_annotation: ^4.7.0 # json 解析依赖
  #  openinstall_flutter_plugin: ^2.2.2
  flutter_easyrefresh: ^2.2.2
  badges: ^2.0.3
#  dio_http_cache: ^0.3.0
#  postman_dio: 1.0.4
#  graphql_flutter: ^5.1.0
#  flutter_swiper、carousel_slider # (图片)轮播组件库
#  package_info、url_launcher # 系统库（app相关信息、打电话、发邮件等）
#  pull_to_refresh、flutter_easyrefresh、liquid_pull_to_refresh # 下拉刷新库
#  infinite_scroll_pagination # 上拉加载
#  flutter_spinkit、loading_indicator、
#  loading_animations: ^2.2.0 # 加载指示器
#  bot_toast、toast、fluttertoast、oktoast、flutter_styled_toast # 土司弹框提示
#  adaptive_dialog、giffy_dialog # 弹框提示
#  fluro、auto_route # 页面路由
#  flutter_keyboard_visibility、keyboard_utils # 键盘相关
#  city_pickers # 省市区选择器
#  image_picker、multi_image_picker2、flutter_wechat_assets_picker（推荐：防wx） # 图片选择器
#  photo_view # 图片预览
#  path_provider # 获取本机文件
#  image_gallery_saver # 保存图片至本机
#  flutter_downloader # 服务端下载
#  sn_progress_dialog # 进度条
#  permission_handler # 权限管理相关
#  fluwx # 微信SDK相关
#  cached_network_image # 网络图片缓存
#  flutter_cache_manager # 文件缓存管理
#  flutter_slidable # 列表左右滑弹出按钮
#  flutter_native_splash、introduction_screen、intro_slider、animated_splash # 启动屏
#  event_bus、scoped_model # 事件传递（跨组件传递数据）
#  sliding_up_panel、modal_bottom_sheet # 底部弹出框
#  custom_navigation_bar、convex_bottom_bar（推荐）、curved_navigation_bar、persistent_bottom_nav_bar、animated_bottom_navigation、animated_bottom_navigation_bar # 底部bar按钮
#  device_preview # 真机预览
#  flutter_html # html渲染
#  flutter_animator、flutter_staggered_animations、animations、simple_animations、lottie # 动画库
#  badges # 角标、标记
#  table_calendar # 日历
#  pin_code_fields # 验证码输入
#  image_cropper # 图片裁剪
#  flutter_rating_bar、smooth_star_rating # 评分评价
#  flutter_blurhash # 高斯模糊
#  like_button # 动态按钮（点赞收藏）
#  flutter_datetime_picker # 时间日期
#  upgrader # app升级
#  socket_io_client # io流
#  flutter_custom_clippers # 组件裁剪
#  wave # 波浪纹
#  flutter_app_badger # app数字角标
#  implicitly_animated_reorderable_list # 列表拖动
#  flutter_colorpicker # 取拾器
#  jiffy # 日期格式化相关
#  readmore # 读取更多内容
#  circular_menu # 旋转菜单
#  draggable_scrollbar # 自定义滚动条
#  flutter_slider_drawer、collapsible_sidebar # 侧边栏
#  popover # 提示
#  qrscan # 扫描
#  flutter_bmflocation # 百度定位插件
#  flutter_baidu_mapapi_map # 百度地图基础地图插件
#  amap_flutter_location # 高德地图定位插件
#  amap_flutter_map # 高德地图基础地图插件
#  camera #相机插件
#  flutter_update_dialog #更新app弹框
#  open_file #打开文件
#  video_compress_ds #视频压缩
#  flutter_luban #图片压缩
#  ————————————————
#  版权声明：本文为CSDN博主「就是三木啊」的原创文章，遵循CC 4.0 BY-SA版权协议，转载请附上原文出处链接及本声明。
#  原文链接：https://blog.csdn.net/weixin_43783419/article/details/125905358

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.1
  build_runner: ^2.2.1
  json_serializable: ^6.3.2
#D:\code\SDK\flutter_windows_3.3.1-stable\bin\flutter packages pub run build_runner watch

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - images/warning.png
    - images/wating.png
    - images/success.png
    - images/check-circle-filled.png
    - images/拨号.png
    - images/phone-incoming.png
    - images/呼入-失败.png
    - images/phone-outgoing.png
    - images/呼出-失败.png
    - images/im_call.caf
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
